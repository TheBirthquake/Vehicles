package vehicle;

import property.HasPropeller;
import property.HasWheels;
import property.HasWings;
import property.CarriesCargo;

public class Plane extends Vehicle implements HasWheels, HasPropeller, HasWings, CarriesCargo {
    //Самолет
}
